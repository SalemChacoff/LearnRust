name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: [adder, closure]
        
    steps:
      - uses: actions/checkout@v4
        if: github.event_name != 'pull_request'
      - name: Build
        run: |
          echo $(ls)
          cd LearnRust
      - name: Run tests
        run: |
          cd LearnRust
          echo $(pwd)
